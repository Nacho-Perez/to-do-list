<tr>
    <td scope="row">
        <div class="col-md-12">
            <h5><%= link_to task.description, task,  remote: true, method: :delete, data: { confirm: '¿Estás segur@?' } %></h5>
            <!-- <span><%= task.deadline.strftime('%-d de %B, %Y a las %H:%Mh') %></span> -->
            <span><%= l(task.deadline, format: '%-d de %B de %Y, a las %H:%Mh') %></span>
            <span class="pull-right">
                <%= link_to fa_icon("edit"), edit_task_path(task), remote: true %>
            </span>
        </div>  
    </td>
</tr>
